{
  "name": "kapture-mock-webhook-server",
  "version": "1.0.0",
  "description": "Mock webhook server for Kapture Finance Maya voicebot — handles Vapi tool calls",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  }
}
