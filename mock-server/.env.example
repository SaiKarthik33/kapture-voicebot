# Port the mock webhook server listens on.
# Vapi will POST tool calls to: http://localhost:PORT/webhook
# When exposed via ngrok, the public URL will be: https://<subdomain>.ngrok-free.app/webhook
PORT=3000
